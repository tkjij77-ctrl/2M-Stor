# ☁️ دليل الإعداد السحابي — آل السيد (Supabase مجاني)

> الهدف: نقل نظام آل السيد من "جهاز واحد" إلى **نظام محلي سحابي** —
> أجهزة غير محدودة، ومتابعة المبيعات من موبايلك في أي وقت،
> بنسخ احتياطي حقيقي، **بتكلفة صفر**.

---

## ⏱️ الوقت المتوقع: 10-15 دقيقة

### الخطوة 1 — حساب Supabase (دقيقتان)
1. روح على [supabase.com](https://supabase.com) وسجّل حساب **مجاناً** (إيميل أو GitHub)
2. من لوحة التحكم: **New Project**
3. اسم المشروع: `al-sayed` (أي اسم)
4. كلمة مرور قاعدة البيانات: اختر كلمة قوية **واحفظها في مكان آمن** (هتحتاجها لو حذفت المشروع)
5. الأقرب جغرافياً: **Frankfurt** (أقرب لأوروبا/الشرق الأوسط)
6. استنى دقيقتين لحد ما المشروع يجهز

### الخطوة 2 — تنفيذ المخطط (دقيقة واحدة)
1. من القائمة الجانبية: **SQL Editor** → **New query**
2. انسخ **ملف `cloud-schema.sql` كاملاً** والصقه
3. ضغط **Run**
4. لازم يظهر: `Success. No rows returned` (أو عدّاد الجداول المنفذة)

### الخطوة 3 — بUCKET الصور (دقيقتان)
1. القائمة الجانبية: **Storage** → **New Bucket**
2. الاسم: `products`
3. فعّل **Public bucket** (عشان الصور تظهر في التطبيق)
4. **Create**

> ملاحظة: خطوة إضافية في الـ SQL عملت الـ bucket تلقائياً — لو ظهر مرتين مش مشكلة، اتأكد بس إن الموجود اسمه `products` و Public.

### الخطوة 4 — استنساخ المفاتيح (دقيقة)
1. القائمة الجانبية: **Settings** (الترس) → **API**
2. انسخ حاجتين:
   - **Project URL** (شكله: `https://xxxx.supabase.co`)
   - **anon public key** (سلسلة طويلة)
3. **لا ترسل لي `service_role key` أبداً** — ده مفتاح سري. الـ anon key عام بالتصميم وآمن (الأمان الحقيقي في سياسات RLS اللي مشغولة).

### الخطوة 5 — أرسل لي في المحادثة
```
Project URL:  https://xxxx.supabase.co
anon key:     eyJhbGciOi...
```

---

## 🔐 الأمان — إيه اللي بيحصل في الخلفية

| الطبقة | الحماية |
|---|---|
| المصادقة | Supabase Auth (bcrypt + جلسات) — باسورداتك مش على الأجهزة خالص |
| الصلاحيات | **Row Level Security**: العميل يقدر يقرأ الأصناف ويعمل فاتورة بس — لا يقدر يعدّل سعر ولا يحذف ولا يشوف الإعدادات حتى لو كسر المتصفح |
| الأدوار | حساب **مدير واحد** مُنشأ مسبقاً على السحابة — أي تسجيل جديد = عميل، والترقية من "إدارة الحسابات" بواسطة المدير |
| النسخ الاحتياطي | قاعدة Postgres حقيقية (مش ملف على المتصفح) — البيانات مش بتروح مع مسح بيانات الموبايل |

---

## 📱 بعد ما نخلص التوصيلة (أنت هتشوف)

- **تسجيل دخول حقيقي** بالإيميل (يشتغل على أي جهاز)
- **مزامنة فورية**: عامل بيع عند الكاونتر + أنت تشوف المبيعات لحظياً من موبايلك
- **أوفلاين**: لو النت قطع، التطبيق يكمل من كاش الجهاز و**يرسل اللي اتعمل أول ما النت يرجع**
- **الواجهة الحالية 100% هتفضل زي ما هي** — بنبدل بس "مكان حفظ البيانات"

---

## ⚠️ حدود الباكيت المجاني (بصراحة)

|resource|الحد المجاني|كفايتك|
|---|---|---|
| قاعدة البيانات | 500MB | كتير (بياناتك الحالية ~500KB) |
| التخزين (صور) | 1GB | كفاية مئات الصور المضغوطة |
| المستخدمون | غير محدود فعلياً | ✅ |
| النشاط | المشروع **بيPause بعد 7 أيام من غير استخدام** | بس بتشتغل يومياً — هيفضل شغال. لو اتوقف، بتوصلك إيميل وتفعّله بضغطة |

---

## ️ خطة الانتقال الآمنة (إزاي هننقل من غير ما نفقد حاجة)

1. **الوضع الحالي هيفضل شغال 100%** — هنضيف "وضع سحابي" يتفعّل لما تحط الـ URL والـ key
2. **ترحيل البيانات**: أول ما نوصّل، هنرفع الـ 227 صنف + كل الفواتير الحالية مرة واحدة (زرار "ترحيل بياناتي")
3. **تجربة مزدوجة**: أول أسبوعين التطبيق يعمل سحابي + كاش محلي — ولو فيه أي مشكلة، بيفضل شغال من الكاش
4. **لا هنمسح حاجة من جهازك** — الـ localStorage يبقى كاش بس

---

# 🔄 التحديث: النسخة الجديدة (سحابة مدمجة)

## ✅ اللي اتبني في التطبيق دلوقتي
- **اتصال Supabase من جوه التطبيق نفسه**: القائمة ← ⚙️ الإعدادات (كمدبر) ← قسم "☁️ الاتصال السحابي" ← تحط الـ URL والـ key ← **🧪 اختبار الاتصال** ← **💾 حفظ وتفعيل**
- **أول ما تتفعّل**: بياناتك المحلية (227 صنف + كل الفواتير) **تتترحّل للسحابة تلقائياً**
- **مزامنة**: أي تغيير بيتبعت للسحابة خلال ثواني، وأي جهاز تاني شايفه فوراً
- **أوفلاين**: لو النت قطع، التطبيق يكمل شغله، والتغييرات **تترحل أول ما النت يرجع** (شارة 📡 أوفلاين في قائمة البروفايل)
- **حسابات حقيقية**: كل مستخدم يسجل بإيميله، **المدير حساب ثابت محمي على السحابة**، والتاليين أدوارهم يعملها المدير من "إدارة الحسابات"
- **لو حصل أي مشكلة**: زرار "⏹ إيقاف" يرجّعك للتخزين المحلي فوراً (بياناتك السحابية محفوظة)

## ⚠️ خطوتين لازم تتعملا في Supabase قبل ما تبدأ

### 1) تأكد إن المخطط اتنفذ
لو لسه عملت الـ SQL: **SQL Editor** ← الصق `cloud-schema.sql` ← **Run**

### 2) عطّل تأكيد الإيميل (مهم عشان العمال يدخلوا فوراً)
**Authentication** ← **Sign In / Providers** ← **Email** ← شيل علامة ✓ من **"Confirm email"** ← Save
> لو ما عملتش الخطوة دي، كل حساب جديد هيستنى إيميل تأكيد قبل ما يقدر يدخل.

## 🔑 الـ anon key
- تاخده من: **Settings → API → anon public** (بتبدأ بـ `eyJ...`)
- **آمن** إنه يكون عند أي حد (التصميم كله مبني إنه عام — الأمان الحقيقي في سياسات RLS اللي ماشية على الجداول)
- **لا ترسل أبداً**: `service_role key` أو **database password**

## 🚀 طريقة التشغيل (3 خيارات)
1. **مباشر من الملف**: افتح `index.html` (للتجربة السريعة)
2. **GitHub Pages** (موصى به): ارفع الملفات على المستودع `2M-Stor` فعّله Pages → يبقى ليك لينك ثابت تفتحه من أي موبايل
3. **Netlify/Vercel**: اسحب المجلد وساحبه → لينك مجاني

> ⚠️ عشان التثبيت كتطبيق (PWA) والمزامنة من أكتر من جهاز، لازم الموقع يكون **HTTPS** (الخيار 2 أو 3).

---

# 📱 استكشاف الأخطاء: "الجوال مش شايف التحديثات/الحسابات"

## السبب الأكثر شيوعاً: الجوال شغال بنسخة قديمة (كاش)
لو فتحت الموقع على الجوال من قبل، المتصفح (أو التطبيق المثبت) بيحفظ نسخة قديمة **بدون السحابة** —
عندها الحسابات والتغييرات **على الجهاز فقط** ومش بتتزامن خالص.

## الحل خطوة بخطوة
1. **على الجوال**: افتح الموقع → لو طلع توست **"✨ في نسخة جديدة — تحديث دلوقتي"** اضغط عليه
2. لو مظهرش: **اقفل التاب/المتصفح كله** (مش بس Refresh) وافتح اللينك تاني
3. لو جربت كتطبيق مثبت: **احذف التطبيق** من الشاشة الرئيسية (مش تحذف بيانات — التطبيق بس)
   وافتح اللينك من المتصفح
4. **تأكد من النسخة**: شاشة الدخول لازم مكتوب عليها **v3.3**
5. **تأكد من الوضع**: بعد الدخول، في قائمة البروفايل هتلاقي شارة:
   - **☁️ سحابي** = مظبوط (بياناتك بتتزامن)
   - **📱 محلي** = المشكلة! (المش مفعّل السحابة أو نسخة قديمة)
   - **📡 أوفلاين** = النت قطع (البيانات هتترسل أول ما يرجع)
6. لو "محلي": الإعدادات ← ☁️ الاتصال السحابي ← حط الـ URL + الـ key ← اختبار ← تفعيل

## ⚠️ نقطة مهمة جداً عن الحسابات
- **الحسابات المحلية** (admin/admin أو Amel/Amel أو "عميل-XXXX") = **خاصة بكل جهاز على حدة** ومش بتتزامن
- **الحسابات السحابية** (إيميل + كلمة مرور) = **حساب واحد واحد** اشتغل من أي جهاز
- يعني: علشان كمبيوترك وجوالك يتعارفوا، لازم **كلمة المرور تكون سحابية** (تسجيل بإيميل من وضع ☁️ سحابي)
- أول إيميل يتسجل في السحابة = **مدير** تلقائياً — والباقي تسجلهم وتغير أدوارهم من "العاملين والعملاء"

## زر "🔄 مزامنة دلوقتي"
لو عايز تتأكد إن البيانات اتزامنت: الإعدادات ← ☁️ الاتصال السحابي ← **🔄 مزامنة دلوقتي**
وهتلاقي "آخر مزامنة: [الوقت]" — ولو في تغييرات قديمة هتيجي في نفس اللحظة.

## تحديثات المستقبل
من نسخة v3.3: أي نشر جديد **يوصل للجوال تلقائياً** (Service Worker بياخد الصفحة من الشبكة الأول)،
ولو في نسخة أحدث وهي متاحة، التطبيق هيظهر **توست "تحديث دلوقتي"** تلقائياً.
